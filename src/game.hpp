// Includi questo file solo una volta
#pragma once

#include <string>

using namespace std;

struct City {
    string nome;
    int popolazione;
    int buget;
    // TODO: int o struct a parte?
    int data;
};
